# Copy this file to .env at the project root and fill in your keys.
# .env is in .gitignore, so it will never be committed to GitHub.
#
# The dev server (`npm run dev`) and the production preview server
# (`npm run build && npm run preview`) BOTH read this file. The browser
# never sees the raw keys — it only talks to /api/recognize-food and
# /api/boohee, which the local server proxies with the keys attached.
#
# The browser cannot control the system prompt, model name, or any
# generation parameter on /api/recognize-food — the server fixes all of
# those in server/validation.cjs and ignores any client-supplied field.

# Qwen-VL (百炼) API key — required for AI food recognition.
QWEN_API_KEY=

# Qwen-VL model name to call. The server pins this — clients cannot
# override it. Default: qwen3-vl-flash.
QWEN_MODEL=qwen3-vl-flash

# Optional: override the upstream Qwen endpoint (e.g. when proxying
# through your own gateway). Default: dashscope compatible-mode.
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions

# 薄荷 (boohee) X-Api-Key — optional but recommended for nutrition enrichment.
BOOHEE_API_KEY=

# Default daily calorie target / limit, used until the user changes them.
VITE_DAILY_GOAL=2000
VITE_DAILY_LIMIT=2300

# Port for the production preview server (`npm run preview`).
PORT=8000

# Comma-separated list of allowed Origin headers for the preview server.
# Local development defaults are filled in automatically.
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ⚠️ This file is for personal local development only.
# /api/* has NO authentication and NO rate limit. If you ever deploy the
# proxy to a server anyone on the internet can reach, add authentication
# AND rate limiting (e.g. an API token gate, Cloudflare Access, or a
# reverse proxy with throttling). The proxy will burn your keys for any
# visitor otherwise.
